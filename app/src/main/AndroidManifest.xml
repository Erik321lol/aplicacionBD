<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.mamposteria.aplicacionbd">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="AMABISCA"
        
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplicacionBD"
        android:usesCleartextTraffic="true">
        <activity android:name=".Entregar_estado"></activity>
        <activity android:name=".datospedido" />
        <activity android:name=".confirmarpedido" />
        <activity android:name=".productos" />
        <activity android:name=".informacionpedido" />
        <activity android:name=".codigopedidoadmin" />
        <activity android:name=".estado" />
        <activity android:name=".seguimiento" />
        <activity android:name=".inicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>